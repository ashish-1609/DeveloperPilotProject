spring:
  application:
    name: DeveloperPilotProject
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/developerpilotproject?createDatabaseIfNotExist=true
    username: root
    password: root
  sql:
    init:
      mode: always
  batch:
    jdbc:
      initialize-schema: always
  flyway:
    enabled: true
    locations: classpath:db/migrations
    baseline-on-migrate: true
  jpa:
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics

logging:
  level:
    org:
      springframework:
        security: DEBUG
springdoc:
  swagger-ui:
    csrf:
      enabled: false

server:
  port: 8090
